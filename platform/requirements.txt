python-roborock>=4.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0

# 音频感官层 (AudioSensor / Microphone)
# ⚠️  系统依赖（pip 前需先安装）：
#   sudo apt install -y libportaudio2 python3-dev gcc
#   libportaudio2：sounddevice 底层音频引擎（OSError: cannot find PortAudio）
#   python3-dev + gcc：webrtcvad 无 aarch64 预编译 wheel，需本地编译
sounddevice>=0.5.0
webrtcvad>=2.0.10
numpy>=1.26.0

# 本地唤醒词检测（openWakeWord，可选）
# 启用条件：.env 中设置 WAKE_WORD_MODEL=<模型名或路径>
# 首次运行时自动下载预训练模型（~50MB，需联网）；自定义中文模型需额外训练
# 真机测试：python platform/devices/wake_word_test.py --list
#
# ⚠️  树莓派/Linux 上 tflite-runtime 无官方 wheel，必须拆成两步安装：
#   步骤一（跳过 tflite-runtime 依赖）：
#     pip3 install openwakeword --no-deps --break-system-packages -i https://pypi.tuna.tsinghua.edu.cn/simple/ --trusted-host pypi.tuna.tsinghua.edu.cn
#   步骤二（手动补 openwakeword 的实际运行依赖，已在下方声明）：
#     pip3 install -r requirements.txt --break-system-packages -i https://pypi.tuna.tsinghua.edu.cn/simple/ --trusted-host pypi.tuna.tsinghua.edu.cn
# openwakeword 的运行依赖（onnxruntime 已在上方 Piper 部分声明，此处补充其余缺失项）：
scipy>=1.10.0
tqdm>=4.0.0

# 音频效应器 (AudioEffector / Speaker)
# edge-tts：云端 TTS（默认 SPEAKER_TTS_ENGINE=edge-tts）
# piper-tts + onnxruntime：本地 Piper（SPEAKER_TTS_ENGINE=piper）；中文等常需系统 espeak-ng
edge-tts>=7.0.0
piper-tts>=1.2.0
onnxruntime>=1.16.0
soundfile>=0.12.1

# 摄像头采集 (Camera)
opencv-python-headless>=4.8.0

# 静态文件服务 (FastAPI StaticFiles，用于快照图片访问)
aiofiles>=23.0.0

# 激光雷达 (Lidar / LD06，CP2102 USB-TTL 串口)
pyserial>=3.5

# SLAM 建图 (breezyslam，Hector SLAM 风格扫描匹配，无需里程计)
# ⚠️  breezyslam 不在 PyPI，需从源码手动安装：
#   git clone https://github.com/simondlevy/BreezySLAM.git
#   cd BreezySLAM/python && sudo python3 setup.py install
# breezyslam>=0.8.0

# GPIO (树莓派引脚控制，底盘电机 + 摄像头云台 PWM)
# RPi.GPIO 不支持树莓派 5（BCM2712），使用 rpi-lgpio 作为兼容替代
rpi-lgpio>=0.6

# 电源监测 (INA219 电流/电压传感器，I2C)
pi-ina219>=1.3.0

# 互联网搜索 (Web Search Tool)
# trafilatura：网页正文提取（去广告/导航/噪音），供 Agent fetchWebPage 工具使用
# 搜索引擎由 SearXNG Docker 容器提供（见 docker-compose.yml / README），Platform 通过 HTTP 调用
trafilatura>=1.12.0
