python-roborock>=4.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0

# 音频感官层 (AudioSensor)
# ⚠️  系统依赖（pip 前需先安装）：
#   sudo apt install -y libportaudio2 python3-dev gcc
#   libportaudio2：sounddevice 底层音频引擎（OSError: cannot find PortAudio）
#   python3-dev + gcc：webrtcvad 无 aarch64 预编译 wheel，需本地编译
sounddevice>=0.5.0
webrtcvad>=2.0.10
numpy>=1.26.0

# 音频效应器 (AudioEffector)
edge-tts>=7.0.0
soundfile>=0.12.1

# 摄像头采集 (Camera)
opencv-python-headless>=4.8.0

# 静态文件服务 (FastAPI StaticFiles，用于快照图片访问)
aiofiles>=23.0.0

# 激光雷达 (Lidar / LD06，CP2102 USB-TTL 串口)
pyserial>=3.5

# SLAM 建图 (breezyslam，Hector SLAM 风格扫描匹配，无需里程计)
# ⚠️  breezyslam 不在 PyPI，需从源码手动安装：
#   git clone https://github.com/simondlevy/BreezySLAM.git
#   cd BreezySLAM/python && sudo python3 setup.py install
# breezyslam>=0.8.0

# GPIO (树莓派引脚控制，底盘电机 + 摄像头云台 PWM)
# RPi.GPIO 不支持树莓派 5（BCM2712），使用 rpi-lgpio 作为兼容替代
rpi-lgpio>=0.6

# 电源监测 (INA219 电流/电压传感器，I2C)
pi-ina219>=1.3.0

# 互联网搜索 (Web Search Tool)
# trafilatura：网页正文提取（去广告/导航/噪音），供 Agent fetchWebPage 工具使用
# 搜索引擎由 SearXNG Docker 容器提供（见 docker-compose.yml / README），Platform 通过 HTTP 调用
trafilatura>=1.12.0
